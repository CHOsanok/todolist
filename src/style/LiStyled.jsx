import styled from "styled-components";

export const Li = styled.li`
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  width: 25rem;
  cursor: pointer;
  padding: 10px;
  font-size: 1.2rem;
`;
